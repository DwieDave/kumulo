{
  "name": "kumulo",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "David Stahl-Gruber",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DwieDave/kumulo.git"
  },
  "homepage": "https://github.com/DwieDave/kumulo#readme",
  "bugs": "https://github.com/DwieDave/kumulo/issues",
  "packageManager": "bun@1.3.13",
  "workspaces": {
    "packages": [
      "packages/*",
      "tools/*"
    ],
    "catalog": {
      "effect": "4.0.0-beta.101",
      "@effect/platform-bun": "4.0.0-beta.101",
      "@effect/platform-node": "4.0.0-beta.101",
      "@effect/openapi-generator": "4.0.0-beta.101",
      "@effect/vitest": "4.0.0-beta.101",
      "vitest": "4.1.10",
      "fast-check": "4.9.0",
      "@types/bun": "1.3.14",
      "typescript": "7.0.2",
      "@effect/tsgo": "0.24.3",
      "dependency-cruiser": "18.1.0",
      "@swc/core": "1.15.46",
      "oxlint": "1.75.0",
      "yaml": "2.9.0",
      "ssh2": "1.16.0",
      "@types/ssh2": "1.15.5"
    }
  },
  "scripts": {
    "typecheck": "for d in packages/* tools/*; do (cd \"$d\" && echo \"[typecheck] $d\" && ../../node_modules/.bin/tsc --noEmit) || exit 1; done",
    "test": "vitest run",
    "lint:deps": "depcruise --config .dependency-cruiser.cjs packages tools",
    "codegen:check": "cd tools/codegen && bun run check",
    "ci": "bun run typecheck && bun run build && bun run test && bun run lint:deps && bun run lint && bun run codegen:check && bun run schema:check",
    "lint": "oxlint",
    "build": "set -e; for d in $(node -e 'const fs=require(\"node:fs\");const p=fs.readdirSync(\"packages\").filter(d=>fs.existsSync(`packages/${d}/package.json`)&&require(`${process.cwd()}/packages/${d}/package.json`).scripts?.build);const m=p.filter(d=>d!==\"core\"&&d!==\"cli\").sort();console.log([...p.filter(d=>d===\"core\"),...m,...p.filter(d=>d===\"cli\")].join(\" \"))'); do echo \"[build] $d\"; bun run --cwd \"packages/$d\" build; done",
    "schema:generate": "bun scripts/generate-schema.ts",
    "schema:check": "set -e; cp kumulo.schema.json .kumulo.schema.check.bak; bun scripts/generate-schema.ts; s=0; diff -u .kumulo.schema.check.bak kumulo.schema.json || { s=$?; echo 'kumulo.schema.json is stale: run `bun run schema:generate` and commit'; }; mv .kumulo.schema.check.bak kumulo.schema.json; exit $s",
    "version:bump": "node scripts/bump-version.mjs",
    "release:rewrite-deps": "node scripts/rewrite-workspace-deps.cjs",
    "specs:update:ovh": "bash tools/ovh2openapi/scripts/update-specs.sh",
    "specs:update:openstack": "bash packages/openstack/scripts/update-specs.sh",
    "specs:update:hetzner": "bash packages/hetzner/scripts/update-specs.sh"
  },
  "devDependencies": {
    "@effect/platform-node": "catalog:",
    "@effect/tsgo": "catalog:",
    "@kumulo/core": "workspace:*",
    "@kumulo/storage-ovh": "workspace:*",
    "@swc/core": "catalog:",
    "@types/bun": "catalog:",
    "dependency-cruiser": "catalog:",
    "effect": "catalog:",
    "oxlint": "catalog:",
    "tsdown": "^0.22.14",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
