import { packageName as core } from "@kumulo/core"

/** Placeholder export proving workspace linking (cli -> core) compiles; real CLI lands in M4. */
export const packageName = "@kumulo/cli"
export const coreDependency = core
